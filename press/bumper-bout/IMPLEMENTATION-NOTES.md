# Bumper Bout press kit launch notes

- Replace the trailer iframe URL in `index.html` at the marked `TODO (launch)` comment when the final demo-launch trailer is available.
- The current download section uses the original SVG and PNG assets already present in the website repository. A dedicated logo-only Bumper Bout lockup is not currently available; add one here later if press usage requires it.
- The trailer caption links to the supplied Google Drive file for downloading without duplicating a large video file in the GitHub Pages repository. Update that link if the launch file changes.
- A downloadable press ZIP can be added later if supplied.
- The screenshot gallery is intentionally data-light: copy an existing `<figure class="press-shot">` block to add future launch screenshots without changing JavaScript.
