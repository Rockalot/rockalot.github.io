# Rockalot website

A small, dependency-free static website for Rockalot and Bumper Bout.

## Preview locally

From this folder, run:

```powershell
python -m http.server 8000
```

Then open <http://localhost:8000>.

The site can also be opened directly from `index.html`, though a local web server is recommended for final checks.
